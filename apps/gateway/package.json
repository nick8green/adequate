{
  "name": "gateway",
  "version": "0.0.0",
  "scripts": {
    "build": "tsup",
    "dev": "nodemon --watch 'src/**/*' --exec \"ts-node --project ./tsconfig.json -r tsconfig-paths/register ./src/index.ts\" --ext js,json,ts"
  },
  "license": "ISC",
  "dependencies": {
    "@apollo/gateway": "2.11.2"
  }
}
