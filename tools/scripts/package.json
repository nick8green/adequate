{
  "name": "scripts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.cli.json",
    "clean": "rm -rf dist",
    "generate-css": "ts-node -r tsconfig-paths/register -P tsconfig.cli.json src/scripts/themes/generate-css.cli.ts",
    "prebuild": "yarn clean"
  }
}
