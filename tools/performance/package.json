{
  "name": "performance",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "artillery": "^2.0.31",
    "artillery-plugin-publish-metrics": "^2.36.0"
  },
  "scripts": {
    "test": "artillery run --output report.json"
  }
}
