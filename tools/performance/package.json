{
  "name": "performance",
  "version": "0.0.0",
  "dependencies": {
    "artillery": "^2.0.23",
    "artillery-plugin-publish-metrics": "^2.28.0"
  },
  "scripts": {
    "test": "artillery run --output report.json"
  }
}
